# VIP 权益批量操作用户选择设计

## 1. 背景

权益台账页面的“人工开通/续期”和“会员补偿”弹窗目前要求管理员手工输入微信用户编号和业务编号。该交互容易选错用户，也不适合一次处理多个用户。

本次调整将两个弹窗统一改为微信用户搜索多选，并由系统自动管理业务编号。现有会员套餐、有效期顺延、赠送积分、补偿不赠送积分及权益台账逻辑保持不变。

## 2. 目标

- 人工开通/续期支持选择一个或多个启用状态的微信用户。
- 会员补偿支持选择一个或多个启用状态的微信用户。
- 用户可按昵称或用户编号搜索。
- 管理员不再手工填写业务编号。
- 批量操作保持幂等，重复提交同一批次不重复续期或赠送积分。
- 一个批次内任意用户处理失败时整批回滚。
- 权益台账继续保留业务编号，满足审计、追踪和防重复处理要求。

## 3. 非目标

- 不修改微信用户与若依 `sys_user` 相互独立的边界。
- 不修改会员套餐价格、有效天数和赠送积分。
- 不修改支付开通、会员码兑换或退款流程。
- 不新增数据库表，不执行数据库迁移。
- 不删除权益台账中的来源和业务编号字段。

## 4. 业务编号的实际意义

`wl_vip_entitlement` 使用 `source_type + source_biz_no` 唯一约束识别一次权益发放。服务在发放权益前先按这两个字段查询：

- 已存在且属于同一用户时，直接返回已有记录，不再次续期或赠送积分。
- 已存在但属于其他用户时，拒绝处理。
- 不存在时，才更新会员到期时间、发放套餐积分并写入权益台账。

因此业务编号同时承担：

1. 幂等控制，防止重复点击或网络重试造成重复发放。
2. 来源追踪，定位权益来自支付订单、会员码、人工开通或补偿。
3. 审计关联，便于后台排查一次操作对应的权益记录。

人工操作不需要管理员理解或填写该技术标识，但系统必须继续生成和保存。

## 5. 交互设计

### 5.1 用户选择

两个弹窗都将“用户编号”输入框改为远程搜索多选框：

- 打开下拉框时加载第一批启用用户。
- 输入昵称时进行模糊搜索。
- 输入纯数字时同时按用户编号精确搜索。
- 每个选项显示：`昵称（用户编号：ID，会员到期：日期）`。
- 未设置昵称时显示“未设置昵称”。
- 未开通会员时显示“未开通”。
- 只返回并允许选择 `status = 0` 的微信用户。
- 已选择用户在后续远程搜索时继续保留，不能因选项刷新丢失。
- 单次最多选择 100 位用户。

用户候选数据只包含用户编号、昵称和会员到期时间，不返回 OpenID、头像路径、积分等无关字段。

### 5.2 人工开通/续期

弹窗字段：

- 微信用户：必填、多选。
- 会员套餐：必填、单选。
- 赠送积分：只读，取当前套餐快照。
- 操作原因：必填。

弹窗不再展示业务编号。一次提交中所有用户使用相同套餐和操作原因。

### 5.3 会员补偿

弹窗字段：

- 微信用户：必填、多选。
- 补偿天数：必填，必须大于 0。
- 赠送积分：只读并固定显示 0。
- 操作原因：必填。

弹窗不再展示业务编号。一次提交中所有用户使用相同补偿天数和操作原因。

### 5.4 提交状态

- 提交期间禁用确认按钮，防止连续点击。
- 成功后关闭弹窗、刷新权益台账，并提示“已成功处理 N 位用户”。
- 失败时保留弹窗和已选内容，显示后端返回的简体中文错误。

## 6. 接口设计

### 6.1 VIP 用户候选接口

新增：

`GET /library/vip-operation/user-options`

权限：

`library:vip:operation`

请求参数：

- `keyword`：可选，昵称关键字或纯数字用户编号。
- `pageNum`：默认 1。
- `pageSize`：默认 20，最大 100。

返回分页数据，每项仅包含：

```json
{
  "id": 12,
  "nickname": "张三",
  "vipExpireTime": "2026-08-28 12:00:00"
}
```

该接口不复用 `/library/wx-user/list` 的权限，避免拥有会员操作权限的人员还必须额外拥有微信用户管理权限。

### 6.2 批量人工开通/续期

保留接口路径：

`POST /library/vip-operation/open`

请求调整为：

```json
{
  "userIds": [12, 18],
  "planId": 3,
  "batchNo": "批次标识",
  "reason": "线下购买"
}
```

### 6.3 批量会员补偿

保留接口路径：

`POST /library/vip-operation/compensate`

请求调整为：

```json
{
  "userIds": [12, 18],
  "days": 7,
  "batchNo": "批次标识",
  "reason": "服务中断补偿"
}
```

### 6.4 批次标识

前端在每次打开弹窗时生成一个隐藏的 20 位字母数字批次标识。提交失败后重试沿用原批次标识，关闭并重新打开弹窗时生成新标识。

后端校验批次标识格式，但不直接使用它作为业务编号。每位用户的业务编号分别生成：

- 人工开通：`MANUAL:<batchNo>:<userId>`
- 会员补偿：`COMPENSATION:<batchNo>:<userId>`

20 位批次标识加最大长度用户编号后仍不超过现有 `source_biz_no varchar(64)` 限制。

### 6.5 返回结果

两个批量接口成功时返回：

```json
{
  "processedCount": 2
}
```

不向前端返回完整权益记录列表，避免不必要的数据传输。

## 7. 后端处理流程

1. 校验请求、用户列表、批次标识、套餐或补偿天数及操作原因。
2. 对用户编号去重并按升序处理，减少并发批量操作时的锁顺序差异。
3. 限制去重后用户数量为 1 至 100。
4. 人工开通读取一次启用套餐快照。
5. 在同一个事务内逐位用户调用现有权益发放逻辑。
6. 每位用户使用独立的自动业务编号。
7. 任意用户不存在、已停用或处理失败时抛出简体中文错误，整个事务回滚。
8. 同批次重试时，已有的 `source_type + source_biz_no` 记录直接复用，不重复更新权益或积分。

## 8. 权限与安全

- 用户候选接口和两个批量接口统一要求 `library:vip:operation`。
- 只返回选择器所需的最小用户信息。
- 不返回 OpenID 或 UnionID。
- 后端不能信任前端候选列表，提交时仍需逐个校验用户存在且启用。
- 操作日志继续记录当前若依后台操作人。
- 所有请求和错误信息使用简体中文。

## 9. 错误处理

主要错误包括：

- “请选择至少一位微信用户”
- “单次最多选择100位微信用户”
- “批次标识格式不正确”
- “会员套餐编号不能为空”
- “补偿天数不能为空”
- “操作原因不能为空”
- “微信用户不存在”
- “当前账号已停用，请联系管理员”

批量失败只返回首个明确错误，不返回半成功结果。

## 10. 测试设计

### 10.1 后端

- 用户候选接口使用会员操作权限，并只返回启用用户的最小字段。
- 候选用户支持昵称模糊搜索和纯数字编号搜索。
- 人工开通请求必须包含用户列表、套餐、批次标识和原因。
- 会员补偿请求必须包含用户列表、天数、批次标识和原因。
- 重复用户编号只处理一次。
- 超过 100 位用户时拒绝处理。
- 批量人工开通为每位用户生成不同业务编号并赠送套餐积分。
- 批量补偿为每位用户生成不同业务编号且赠送积分为 0。
- 同一批次重复提交不重复续期或赠送积分。
- 中间任意用户失败时整个批次回滚。

### 10.2 管理端

- 两个弹窗均不再出现用户编号输入框和业务编号输入框。
- 两个弹窗均使用远程搜索多选。
- 选择器请求 VIP 专用用户候选接口。
- 请求体提交 `userIds` 和隐藏 `batchNo`。
- 提交期间确认按钮处于加载和禁用状态。
- 成功提示包含实际处理人数。

## 11. 兼容性

本次直接更新后台管理端与对应管理接口，不保留旧的单个 `userId + bizNo` 请求格式。小程序、支付回调、会员码兑换和其他会员接口不使用这两个管理端接口，因此不受影响。
