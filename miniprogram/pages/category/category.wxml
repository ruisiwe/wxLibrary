<view class="list">
  <document-row wx:for="{{items}}" wx:key="id" document="{{item}}" show-file-type="{{true}}" />
  <state-view loading="{{loading}}" error="{{error}}" empty="{{!loading && !error && !items.length}}" bindretry="load" />
</view>
