<view class="list"><document-row wx:for="{{items}}" wx:key="id" document="{{item}}"/><state-view loading="{{loading}}" error="{{error}}" empty="{{!loading && !error && !items.length}}" empty-text="暂无收藏" bindretry="load"/></view>
