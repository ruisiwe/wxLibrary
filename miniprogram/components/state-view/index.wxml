<view wx:if="{{loading || error || empty}}" class="state">
  <block wx:if="{{loading}}">加载中…</block>
  <block wx:elif="{{error}}">
    <text>{{error}}</text>
    <button size="mini" bindtap="retry">重试</button>
  </block>
  <block wx:elif="{{empty}}">{{emptyText}}</block>
</view>
