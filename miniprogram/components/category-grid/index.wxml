<view class="grid">
  <view wx:for="{{items}}" wx:key="id" class="grid__item" data-id="{{item.id}}" bindtap="open">
    <image class="grid__icon" src="{{item.iconUrl}}" mode="aspectFit" />
    <text>{{item.name}}</text>
  </view>
</view>
